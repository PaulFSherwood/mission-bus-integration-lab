# MBIL DIS Captures

This folder is for local DIS capture and replay files.

Do not commit capture files to GitHub. They can grow large and are machine/test-run specific.

Keep only this README and `.gitkeep`.
